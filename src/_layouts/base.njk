<!DOCTYPE html>
<html lang="en">
  {%- set pageTitle %}
  {% if title %}{{ title }} | {% endif %}{{ meta.siteName }}{% endset -%}
  {%- set pageDescription %}
  {% if description %}{{ description }}
  {% else %}{{ meta.siteDescription }}
  {% endif %}{% endset -%}
  {%- set pageSocialImg %}{{ meta.url }}/previews/{% if title %}{{ title | slug }}
  {% else %}home{% endif %}.png{% endset -%}
  <head>

    <!-- Generated from the 11ty Netlify Jumpstart: https://github.com/5t3ph/11ty-netlify-jumpstart -->
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content="eleventy" />
    <title>{{pageTitle}}</title>
    <meta
      name="description"
      content="{{pageDescription}}"
    <link rel="icon" href="{{ '/favicon.png' | url }}" />
    <link href="{{ '/css/reset.css' | url }}" rel="stylesheet" />
    <link href="{{ '/css/style.css' | url }}" rel="stylesheet" />
    <meta name="twitter:card" content="summary_large_image" />
    {% if meta.twitterUsername %}
      <meta name="twitter:creator" content="@{{ meta.twitterUsername }}" />
    {% endif %}
    <meta
      name="twitter:title"
      content="{{pageTitle}}"
    <meta
      name="twitter:description"
      content="{{pageDescription}}"

    <meta
      name="og:title"
      content="{{pageTitle}}"
    <meta
      name="og:description"
      content="{{pageDescription}}"
    <meta property="og:image" content="{{pageSocialImg}}" />
    <meta name="twitter:image" content="{{pageSocialImg}}" />

    <link
      rel="alternate"
      type="application/rss+xml"
      title="RSS Feed for {{ meta.siteName }}"
      href="/feed/"
    <link rel="webmention" href="https://webmention.io/ritikpatni.me/webmention" />
    <link rel="pingback" href="https://webmention.io/ritikpatni.me/xmlrpc" />
  </head>
  <body>
    {{ content | safe }}

    <footer class="footer">
      <ul role="list" class='footer__links'>
        {% for link in collections.pages %}
        <li>
          <a {% if currentUrl === link.url %} aria-current="page" {% endif %} href="{{ link.url }}" class="footer__links__link">
            {{ link.data.title }}
          </a>
        </li>
      {%- endfor %}
      </ul>
    </footer>
  </body>
</html>